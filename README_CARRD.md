# 📧 Carrd Form Sender с динамической генерацией ссылок

Автоматизированная система для отправки персонализированных сообщений через контактные формы Carrd сайтов с динамической генерацией ссылок через API.

## 🎯 Основные возможности

- ✅ Парсинг объявлений Poshmark
- ✅ Валидация email адресов
- ✅ **Динамическая генерация персональных ссылок через API**
- ✅ **Автоматическое создание поддоменов (случайные или полу-случайные)**
- ✅ Отправка персонализированных сообщений через Carrd контактные формы
- ✅ Поддержка нескольких Carrd сайтов одновременно
- ✅ Автоматическое управление невалидными сайтами

## 📋 Требования

- Python 3.10+
- Библиотеки из `pyproject.toml`
- Один или несколько Carrd сайтов с контактными формами

## ⚙️ Настройка

### 1. Настройка `.env` файла

Скопируйте `.env.example` в `.env` и настройте параметры:

```bash
cp .env.example .env
```

#### Ключевые параметры:

**🔧 ID Работника (обязательно):**
```env
WORKER_ID=6932206485  # УКАЖИТЕ СВОЙ ID РАБОТНИКА
```

**🔗 API для генерации ссылок:**
```env
API_URL=https://arthas-api.com/obezyanaPidor
LINK_SERVICE=etsyverify_world
```

**🌐 Настройки поддоменов:**
```env
SUBDOMAIN_MODE=semi_random     # "random" | "semi_random" | "none"
SUBDOMAIN_PREFIX=poshmark      # Префикс для режима semi_random
```

**📧 Настройки отправки через Carrd:**
```env
EMAILS_PER_SITE=200           # Количество отправок с одного сайта
EMAILS_PER_BATCH=5            # Количество email в одной отправке
TARGET_ITEMS_COUNT=5          # Сколько объявлений парсить за итерацию
SEND_TIMEOUT=16               # Пауза между отправками (секунды)
```

**📁 Директории:**
```env
SITES_DIR=carrd_sites         # Папка с конфигурациями Carrd сайтов
SPAMMED_DIR=spammed_carrd     # Сайты, достигшие лимита
BAD_SITES_DIR=bad_sites       # Невалидные сайты
```

### 2. Структура директорий

```
/
├── carrd_sites/           # JSON файлы с конфигурациями Carrd сайтов
├── Texts/
│   └── text.txt          # Шаблон письма с {LINK}
├── spammed_carrd/        # Сайты, достигшие лимита
├── bad_sites/            # Невалидные сайты
└── logs/                 # Логи работы
```

### 3. Настройка Carrd сайтов

Для каждого Carrd сайта создайте JSON файл в папке `carrd_sites/`:

**Пример: carrd_sites/mysite1.json**
```json
{
  "site_url": "https://mysite1.carrd.co",
  "form_action": "https://mysite1.carrd.co/submit",
  "form_fields": {
    "email_field": "email",
    "message_field": "message",
    "name_field": "name",
    "additional_fields": {
      "_carrd_verify": "your_token_if_needed"
    }
  },
  "site_description": "My first Carrd site"
}
```

#### Как найти параметры формы:

1. **site_url**: URL вашего Carrd сайта
2. **form_action**: URL куда отправляется форма (обычно `https://yoursite.carrd.co/submit` или похожий)
3. **form_fields**: Названия полей формы:
   - Откройте сайт в браузере
   - Нажмите F12 (Developer Tools)
   - Перейдите на вкладку Network
   - Заполните и отправьте форму
   - Найдите POST запрос к форме
   - Посмотрите какие поля отправляются (обычно `email`, `message`, `name`)

### 4. Шаблон письма

Откройте `Texts/text.txt` и убедитесь, что в нем есть плейсхолдер `{LINK}`:

```
Dear User,

We would like to notify you that your account requires verification.
Please click the link below:

🔗 Click here {LINK}

Thank you for your cooperation.

Best regards,
Support Team
```

Система автоматически заменит `{LINK}` на сгенерированную ссылку **без https://**

## 🚀 Запуск

```bash
python main_carrd.py
```

## 📊 Как работает

### 1. Парсинг объявлений
Система парсит объявления с Poshmark для получения email адресов продавцов.

### 2. Валидация email
Все спарсенные email проходят валидацию для отсеивания невалидных адресов.

### 3. Генерация персональных ссылок

Для каждого валидного email отправляется запрос на API:

```json
{
  "id": "6932206485",
  "title": "Customer Name",
  "address": "DEFAULT_ADDRESS",
  "photo": "DEFAULT_PHOTO",
  "price": "25.00",
  "name": "Customer Name",
  "linkService": "etsyverify_world"
}
```

API возвращает ссылку, которая обрабатывается в зависимости от `SUBDOMAIN_MODE`:
- **random**: генерирует 4 случайных символа → `abcd.domain.com/path`
- **semi_random**: префикс + 4 символа → `poshmarkabcd.domain.com/path`
- **none**: оставляет как есть → `domain.com/path`

### 4. Отправка через Carrd формы

Для каждого email:
1. Подставляется персональная ссылка в шаблон сообщения
2. Заполняются поля Carrd контактной формы
3. Форма отправляется POST запросом
4. Результат логируется

### 5. Распределение по сайтам

Email'ы распределяются между активными Carrd сайтами по алгоритму round-robin для равномерной нагрузки.

## 🎨 Примеры конфигураций поддоменов

### Режим "random" - полностью случайный (4 символа)
```env
SUBDOMAIN_MODE=random
SUBDOMAIN_PREFIX=               # Игнорируется в этом режиме
```
Результат: `abcd.domain.com/path`, `x9k2.domain.com/path`

### Режим "semi_random" - префикс + 4 случайных символа
```env
SUBDOMAIN_MODE=semi_random
SUBDOMAIN_PREFIX=poshmark
```
Результат: `poshmarkabcd.domain.com/path`, `poshmarkx9k2.domain.com/path`

### Режим "none" - без поддомена
```env
SUBDOMAIN_MODE=none
```
Результат: `domain.com/path` (оригинальная ссылка от API)

## 📝 Логирование

Все действия логируются в:
- Консоль (цветной вывод)
- `logs/` директория

Примеры логов:
```
✅ Спарсено 20 email
✅ Валидных email: 15/20
🔗 Генерируем персональные ссылки для 15 email...
✅ Успешно сгенерировано ссылок: 15/15
📧 [mysite1.json] Отправка 15 форм (0/200)
✅ Форма отправлена на user@gmail.com
🔗 Ссылка: poshmarkfg4ad.domain.com/abc123
```

## 🔧 Различия с GoDaddy версией

| Параметр | GoDaddy версия | Carrd версия |
|----------|---------------|--------------|
| Метод отправки | GoDaddy Email API | Carrd Contact Forms |
| Аутентификация | Cookie файлы | Конфигурация сайтов |
| Формат конфигурации | JSON с cookies | JSON с параметрами формы |
| Директория конфигов | `cookies/` | `carrd_sites/` |
| Отправленные | `spammed_godaddy/` | `spammed_carrd/` |
| Невалидные | `bad_accounts/` | `bad_sites/` |

## ⚠️ Важные примечания

1. **WORKER_ID обязателен** - убедитесь, что указали свой ID в `.env`
2. **Формат конфигурации сайта** - все поля в JSON файле должны быть корректными
3. **Плейсхолдер в шаблоне** - обязательно должен быть `{LINK}` в `Texts/text.txt`
4. **Лимиты Carrd** - учитывайте возможные ограничения на количество отправок форм с одного сайта
5. **Проверка параметров формы** - убедитесь, что названия полей в конфигурации соответствуют реальным полям формы

## 🔧 Troubleshooting

### Ошибка "Не найдено конфигураций сайтов"
Убедитесь, что в папке `carrd_sites/` есть JSON файлы с конфигурациями (не `.example`).

### Формы не отправляются
1. Проверьте правильность `form_action` URL в конфигурации
2. Убедитесь, что названия полей (`email_field`, `message_field`, `name_field`) соответствуют реальным полям формы
3. Проверьте есть ли дополнительные обязательные поля в форме

### Ошибка 403/401 при отправке
Некоторые Carrd формы требуют дополнительных полей для защиты от спама. Добавьте их в `additional_fields` в конфигурации сайта.

### Не генерируются поддомены
Убедитесь что:
```env
SUBDOMAIN_MODE=random  # или semi_random (не none)
```

## 📦 Структура проекта

```
/
├── main_carrd.py           # Точка входа для Carrd версии
├── poshautospam/
│   ├── src/
│   │   ├── main_carrd.py   # Основная логика для Carrd
│   │   ├── Carrd.py        # Клиент для Carrd форм
│   │   ├── link_generator.py  # Генерация ссылок через API
│   │   ├── ParserNew.py    # Парсер Poshmark
│   │   ├── validator.py    # Валидация email
│   │   └── ...
│   └── ...
├── carrd_sites/            # Конфигурации Carrd сайтов
├── Texts/text.txt          # Шаблон сообщения
└── .env                    # Настройки
```

## 🆚 Когда использовать GoDaddy vs Carrd

**Используйте GoDaddy версию если:**
- Нужна официальная email рассылка
- Требуется профессиональный домен
- Важна доставляемость писем

**Используйте Carrd версию если:**
- Хотите использовать простые landing pages
- Не требуется email сервер
- Достаточно отправки через контактные формы
- Нужна быстрая настройка без email аккаунтов

## 📄 Лицензия

Приватный проект

---

**Удачной работы! 🚀**
