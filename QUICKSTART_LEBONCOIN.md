# 🇷🇺 Краткое руководство по использованию Leboncoin Parser

## Быстрый старт

### 1. Подготовка прокси (опционально)

Создайте файл `proxies.txt` со списком ваших прокси:
```
http://user:pass@proxy1.com:8080
http://user:pass@proxy2.com:8080
socks5://user:pass@proxy3.com:1080
```

### 2. Простой пример парсинга

```python
import asyncio
from src.LeboncoinParser import LeboncoinParser
from src.LeboncoinUtils import load_proxies

async def main():
    # Загружаем прокси
    proxies = load_proxies('proxies.txt')
    
    # Создаем парсер
    parser = LeboncoinParser(
        categories=['voitures', 'informatique'],  # Автомобили, Компьютеры
        proxy_list=proxies,
        max_listings=50,  # Парсить до 50 объявлений
    )
    
    # Запускаем парсинг
    listings = await parser.start_parsing()
    
    # Сохраняем результаты
    parser.save_results('results.json')
    
    print(f"Найдено {len(listings)} объявлений")

asyncio.run(main())
```

### 3. Запуск готового примера

```bash
python leboncoin_example.py
```

## Доступные категории

Самые популярные:
- `voitures` - Автомобили
- `immobilier` - Недвижимость
- `informatique` - Компьютеры
- `telephonie` - Телефоны
- `ameublement` - Мебель
- `electromenager` - Бытовая техника
- `motos` - Мотоциклы

Полный список в файле `src/LeboncoinConfig.py` (64 категории)

## Фильтрация по продавцам

```python
from src.LeboncoinUtils import filter_listings_by_seller

# Настройки фильтра
filters = {
    'min_sales': 10,        # Минимум 10 продаж
    'min_reviews': 5,       # Минимум 5 отзывов
    'min_rating': 4.0,      # Рейтинг не ниже 4.0
    'seller_types': ['pro'] # Только профессиональные продавцы
}

# Применяем фильтр
filtered = filter_listings_by_seller(listings, filters)
```

## Структура данных объявления

Каждое объявление содержит:
```json
{
  "title": "iPhone 15 Pro 256GB",
  "price": 950,
  "location": "Paris 75001",
  "url": "https://www.leboncoin.fr/...",
  "seller_info": {
    "seller_name": "Jean Dupont",
    "seller_type": "pro",
    "sales_count": 25,       // Количество продаж
    "reviews_count": 18,     // Количество отзывов
    "average_rating": 4.5,   // Средний рейтинг
    "response_time": "< 1 час"
  }
}
```

## Параметры парсера

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `categories` | Список категорий для парсинга | Все |
| `proxy_list` | Список прокси | [] |
| `max_listings` | Максимум объявлений | 100 |
| `max_concurrent` | Одновременных запросов | 10 |

## Важные замечания

⚠️ **Внимание:**
1. Используйте прокси для больших объемов (рекомендуется)
2. Не ставьте `max_concurrent` слишком большим (5-10 оптимально)
3. Парсер автоматически добавляет задержки между запросами
4. Соблюдайте условия использования leboncoin.fr

## Получение статистики

```python
from src.LeboncoinUtils import format_seller_stats

stats = format_seller_stats(listings)
print(f"Всего объявлений: {stats['total_listings']}")
print(f"Уникальных продавцов: {stats['total_sellers']}")
print(f"Средний рейтинг: {stats['avg_rating']:.2f}")
print(f"Профессиональных продавцов: {stats['pro_sellers']}")
```

## Сохранение результатов

```python
# В JSON
parser.save_results('results.json')

# Или вручную
import json
with open('filtered.json', 'w', encoding='utf-8') as f:
    json.dump(filtered_listings, f, ensure_ascii=False, indent=2)
```

## Решение проблем

### Не находит объявления
- Проверьте правильность названий категорий
- Попробуйте без прокси
- Уменьшите `max_concurrent`

### Блокировки
- Используйте качественные прокси
- Уменьшите скорость парсинга
- Добавьте больше задержек

### Ошибки парсинга
- Структура сайта могла измениться
- Обновите библиотеки: `pip install --upgrade aiohttp beautifulsoup4 lxml`

## Дополнительная документация

📖 Полная документация: [LEBONCOIN_README.md](LEBONCOIN_README.md)

---

**Удачи в парсинге! 🚀**
