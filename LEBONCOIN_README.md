# 🇫🇷 Leboncoin Parser - Парсер объявлений с leboncoin.fr

Мощный асинхронный парсер для сайта leboncoin.fr с поддержкой прокси, фильтрации по продавцам и множественных категорий.

## 🎯 Основные возможности

- ✅ Парсинг объявлений из множественных категорий
- ✅ Поддержка прокси с автоматической ротацией
- ✅ Извлечение детальной информации о продавцах:
  - Количество продаж
  - Количество отзывов
  - Средний рейтинг
  - Тип продавца (профессионал/частное лицо)
  - Дата регистрации
  - Время ответа
- ✅ Фильтрация результатов по параметрам продавца
- ✅ Асинхронная обработка для высокой производительности
- ✅ Сохранение результатов в JSON формате
- ✅ Подробное логирование процесса

## 📋 Требования

- Python 3.11+
- Библиотеки (установятся автоматически):
  - aiohttp
  - beautifulsoup4
  - lxml
  - loguru
  - certifi

## ⚙️ Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd fdsfsd
```

2. Установите зависимости:
```bash
pip install -r requirements.txt
# или с помощью uv
uv pip install -r requirements.txt
```

3. Настройте `.env` файл (опционально):
```bash
cp .env.example .env
```

## 🚀 Быстрый старт

### Базовое использование

```python
import asyncio
from src.LeboncoinParser import LeboncoinParser

async def main():
    # Создаем парсер
    parser = LeboncoinParser(
        categories=['voitures', 'informatique'],  # Категории для парсинга
        max_listings=50,                          # Максимум объявлений
    )
    
    # Запускаем парсинг
    listings = await parser.start_parsing()
    
    # Сохраняем результаты
    parser.save_results('results.json')

if __name__ == '__main__':
    asyncio.run(main())
```

### Использование с прокси

```python
from src.LeboncoinUtils import load_proxies

# Загружаем прокси из файла
proxies = load_proxies('proxies.txt')

parser = LeboncoinParser(
    categories=['voitures', 'immobilier'],
    proxy_list=proxies,
    max_listings=100,
    max_concurrent=5,  # Одновременных запросов
)
```

### Фильтрация по продавцам

```python
from src.LeboncoinUtils import filter_listings_by_seller

# Настройки фильтра
seller_filters = {
    'min_sales': 10,        # Минимум 10 продаж
    'min_reviews': 5,       # Минимум 5 отзывов
    'min_rating': 4.0,      # Рейтинг не ниже 4.0
    'seller_types': ['pro'] # Только профессиональные продавцы
}

# Применяем фильтр
filtered = filter_listings_by_seller(listings, seller_filters)
```

## 📖 Использование примера

Запустите готовый пример:

```bash
python leboncoin_example.py
```

Этот скрипт демонстрирует:
- Загрузку прокси
- Выбор категорий
- Парсинг объявлений
- Фильтрацию по продавцам
- Сохранение результатов
- Вывод статистики

## 📂 Структура проекта

```
fdsfsd/
├── src/
│   ├── LeboncoinParser.py      # Основной парсер
│   ├── LeboncoinConfig.py      # Конфигурация категорий
│   └── LeboncoinUtils.py       # Утилиты и фильтры
├── leboncoin_example.py        # Пример использования
├── proxies.txt                 # Список прокси (опционально)
├── .env.example                # Пример настроек
└── leboncoin_results/          # Папка с результатами
    ├── all_listings.json       # Все результаты
    ├── filtered_listings.json  # Отфильтрованные
    └── statistics.json         # Статистика
```

## 🗂️ Категории leboncoin

Доступные категории (используйте ключи):

### Транспорт
- `voitures` - Автомобили
- `motos` - Мотоциклы
- `caravaning` - Караванинг
- `utilitaires` - Коммерческий транспорт
- `equipement_auto` - Автозапчасти

### Недвижимость
- `immobilier` - Недвижимость
- `ventes_immobilieres` - Продажа
- `locations` - Аренда
- `colocations` - Совместная аренда

### Электроника
- `multimedia` - Мультимедиа
- `informatique` - Компьютеры
- `telephonie` - Телефония
- `consoles_jeux_video` - Консоли и игры

### Дом
- `maison` - Дом
- `ameublement` - Мебель
- `electromenager` - Бытовая техника
- `decoration` - Декор

### Досуг
- `loisirs` - Досуг
- `velos` - Велосипеды
- `sports_hobbies` - Спорт и хобби

И многие другие! Полный список в `src/LeboncoinConfig.py`

## 🔧 Настройка прокси

Создайте файл `proxies.txt` со списком прокси:

```
http://user:pass@proxy1.example.com:8080
http://user:pass@proxy2.example.com:8080
socks5://user:pass@proxy3.example.com:1080
```

Форматы поддерживаются:
- HTTP/HTTPS прокси
- SOCKS5 прокси
- С авторизацией и без

## 📊 Структура данных объявления

Каждое объявление содержит:

```json
{
  "title": "Название объявления",
  "price": 1000,
  "location": "Paris 75001",
  "category": "Informatique",
  "url": "https://www.leboncoin.fr/...",
  "listing_id": "123456789",
  "image_url": "https://...",
  "date_posted": "Aujourd'hui, 15:30",
  "seller_info": {
    "seller_id": "abc123",
    "seller_name": "Jean Dupont",
    "seller_type": "pro",
    "sales_count": 25,
    "reviews_count": 18,
    "average_rating": 4.5,
    "registration_date": "Membre depuis 2020",
    "response_rate": "95%",
    "response_time": "Répond en moins d'une heure"
  }
}
```

## 🎛️ Параметры парсера

### LeboncoinParser

| Параметр | Тип | По умолчанию | Описание |
|----------|-----|--------------|----------|
| `categories` | List[str] | все | Категории для парсинга |
| `proxy_list` | List[str] | [] | Список прокси |
| `max_listings` | int | 100 | Макс. объявлений |
| `max_concurrent` | int | 10 | Макс. одновременных запросов |

### Фильтры продавца

| Параметр | Тип | Описание |
|----------|-----|----------|
| `min_sales` | int | Минимальное количество продаж |
| `min_reviews` | int | Минимальное количество отзывов |
| `min_rating` | float | Минимальный рейтинг (0.0-5.0) |
| `seller_types` | List[str] | Типы продавцов: 'pro' или 'particulier' |

## 📈 Получение статистики

```python
from src.LeboncoinUtils import format_seller_stats

stats = format_seller_stats(listings)
print(f"Всего объявлений: {stats['total_listings']}")
print(f"Уникальных продавцов: {stats['total_sellers']}")
print(f"Средний рейтинг: {stats['avg_rating']:.2f}")
```

Статистика включает:
- Общее количество объявлений
- Количество уникальных продавцов
- Распределение по типам продавцов
- Средние показатели (рейтинг, отзывы, продажи)

## ⚠️ Важные замечания

1. **Скорость парсинга**: Используйте умеренные значения `max_concurrent` (5-10), чтобы избежать блокировок
2. **Прокси**: Рекомендуется использовать прокси для больших объемов
3. **Задержки**: Между запросами автоматически добавляются задержки
4. **Rate Limits**: Парсер автоматически обрабатывает rate limits (HTTP 429)
5. **Структура сайта**: Селекторы могут измениться при обновлении leboncoin.fr

## 🐛 Решение проблем

### Не находит объявления
- Проверьте названия категорий в `LeboncoinConfig.py`
- Убедитесь, что категория существует на сайте
- Попробуйте уменьшить `max_concurrent`

### Блокировки/капчи
- Используйте качественные прокси
- Уменьшите `max_concurrent`
- Добавьте задержки между запросами

### Ошибки парсинга
- Структура сайта могла измениться
- Проверьте актуальность селекторов в `LeboncoinParser.py`
- Обновите библиотеки: `pip install --upgrade -r requirements.txt`

## 📝 Логирование

Логи сохраняются в:
- Консоль (цветной вывод)
- `leboncoin_parser.log` (файл с ротацией)

Уровни логирования:
- INFO - Основная информация
- SUCCESS - Успешные операции
- WARNING - Предупреждения
- ERROR - Ошибки

## 🔄 Обновления

Следите за обновлениями структуры сайта leboncoin.fr:
- Селекторы могут измениться
- Могут появиться новые категории
- API может обновиться

## 📄 Лицензия

Приватный проект. Использование только в образовательных целях.

## ⚖️ Юридические аспекты

⚠️ **Важно**: 
- Соблюдайте условия использования leboncoin.fr
- Не используйте для коммерческих целей без разрешения
- Уважайте robots.txt и rate limits
- Используйте ответственно

---

**Удачного парсинга! 🚀**
