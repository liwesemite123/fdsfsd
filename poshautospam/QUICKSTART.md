# 🚀 Быстрый старт - Quick Start

## 📋 Минимальная настройка за 3 шага

### Шаг 1: Настройте `.env` файл

Откройте файл `.env` и **ОБЯЗАТЕЛЬНО** укажите свой ID работника:

```env
WORKER_ID=6932206485  # ⚠️ УКАЖИТЕ СВОЙ ID!
```

### Шаг 2: Проверьте шаблон письма

Откройте `Texts/text.txt` и убедитесь, что есть плейсхолдер:

```
🔗 Click here {LINK}
```

### Шаг 3: Добавьте куки аккаунтов

Поместите JSON файлы с куками GoDaddy в папку `cookies/`:

```
cookies/
├── account1.txt
├── account2.txt
└── ...
```

### Запуск:

```bash
python main.py
```

---

## ⚙️ Настройка поддоменов (опционально)

### Вариант 1: Полностью случайный (4 символа)
```env
SUBDOMAIN_MODE=random
```
➡️ Результат: `abcd.domain.com/path`

### Вариант 2: С префиксом "poshmark" + 4 случайных
```env
SUBDOMAIN_MODE=semi_random
SUBDOMAIN_PREFIX=poshmark
```
➡️ Результат: `poshmarkabcd.domain.com/path`

### Вариант 3: Без поддоменов
```env
SUBDOMAIN_MODE=none
```
➡️ Результат: `domain.com/path`

---

## 📊 Что происходит при запуске?

1. ✅ Загружаются аккаунты из `cookies/`
2. 🔍 Парсятся объявления Poshmark
3. ✅ Валидируются email адреса
4. 🔗 **Генерируются персональные ссылки через API**
5. 🌐 **Добавляются случайные поддомены**
6. 📧 Отправляются письма через GoDaddy

---

## 🎯 Пример работы

### Вход (парсер):
```
Username: john_doe
Email: john_doe@gmail.com
Item: Vintage Jacket
```

### Запрос к API:
```json
{
  "id": "6932206485",
  "title": "Vintage Jacket",
  "name": "john_doe",
  "linkService": "etsyverify_world",
  ...
}
```

### API возвращает:
```
https://tracking-domain.com/abc123xyz
```

### Система обрабатывает:
- Удаляет `https://`
- Генерирует поддомен: `abcd` (в режиме random)
- Или добавляет префикс: `poshmark` + `abcd` (в режиме semi_random)

### Финальная ссылка в письме:
```
poshmarkabcd.tracking-domain.com/abc123xyz
```

---

## 📝 Логи

Следите за процессом в консоли:

```
🔍 Парсим 5 объявлений...
✅ Спарсено 5 email
✅ Валидных email: 4/5
🔗 Генерируем персональные ссылки для 4 email...
✅ Успешно сгенерировано ссылок: 4/4
🔗 Используем персональную ссылку: poshmarkabcd.domain.com/abc123
✅ Сообщение отправлено на john_doe@gmail.com
```

---

## ⚠️ Важно!

1. ✅ **WORKER_ID обязателен** - это ваш уникальный ID
2. ✅ **{LINK} в шаблоне** - не забудьте добавить плейсхолдер
3. ✅ **Куки аккаунтов** - должны быть в формате JSON
4. ✅ **Прокси настроены** - проверьте `MAIN_PROXY` в `.env`

---

## 🆘 Проблемы?

### Ссылки не генерируются?
- Проверьте `API_URL` в `.env`
- Убедитесь что API доступен

### Поддомены не добавляются?
- Проверьте: `SUBDOMAIN_MODE=random` или `semi_random`

### Аккаунты невалидны?
- Обновите куки в папке `cookies/`

---

**Готово! Теперь запускайте `python main.py` 🚀**
