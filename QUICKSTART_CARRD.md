# 🚀 Быстрый старт - Carrd версия

Этот гайд поможет быстро настроить и запустить Carrd версию софта.

## 📋 Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

Или если используете `uv`:

```bash
uv pip install -e .
```

## ⚙️ Шаг 2: Настройка .env файла

1. Скопируйте пример:
```bash
cp .env.example .env
```

2. Откройте `.env` и настройте ключевые параметры:

```env
# ОБЯЗАТЕЛЬНО: Ваш Worker ID
WORKER_ID=6932206485

# API для генерации ссылок
API_URL=https://arthas-api.com/obezyanaPidor
LINK_SERVICE=etsyverify_world

# Режим поддоменов
SUBDOMAIN_MODE=semi_random
SUBDOMAIN_PREFIX=poshmark

# Настройки отправки
EMAILS_PER_SITE=200
EMAILS_PER_BATCH=5
TARGET_ITEMS_COUNT=5
SEND_TIMEOUT=16
```

## 🌐 Шаг 3: Настройка Carrd сайта

### 3.1 Создайте Carrd сайт с контактной формой

1. Перейдите на https://carrd.co/
2. Создайте новый сайт
3. Добавьте элемент "Form"
4. Настройте поля формы:
   - Email
   - Message (или Text)
   - Name (опционально)

### 3.2 Получите параметры формы

1. Откройте ваш Carrd сайт в браузере
2. Нажмите F12 (Developer Tools)
3. Перейдите на вкладку "Network"
4. Заполните форму тестовыми данными и отправьте
5. Найдите POST запрос (обычно к `/submit` или похожему)
6. Посмотрите:
   - URL запроса (это будет `form_action`)
   - Названия полей в Form Data (это будут поля в `form_fields`)

### 3.3 Создайте конфигурацию

Создайте файл `carrd_sites/mysite.json`:

```json
{
  "site_url": "https://yoursite.carrd.co",
  "form_action": "https://yoursite.carrd.co/submit",
  "form_fields": {
    "email_field": "email",
    "message_field": "message",
    "name_field": "name",
    "additional_fields": {}
  },
  "site_description": "My Carrd site"
}
```

**Важно:** Замените:
- `yoursite.carrd.co` на ваш реальный домен
- Названия полей на те, что увидели в Developer Tools

## 📝 Шаг 4: Настройка шаблона сообщения

Создайте или отредактируйте `Texts/text.txt`:

```text
Dear User,

We would like to notify you about an important update.
Please click the link below:

🔗 Click here {LINK}

Thank you for your cooperation.

Best regards,
Support Team
```

**Важно:** Не забудьте плейсхолдер `{LINK}` - он будет заменен на персональную ссылку.

## 🚀 Шаг 5: Запуск

```bash
python main_carrd.py
```

## 📊 Что произойдет:

1. Система запарсит объявления с Poshmark
2. Проверит валидность email адресов
3. Сгенерирует персональные ссылки для каждого email
4. Отправит формы через ваш Carrd сайт
5. Будет логировать процесс в консоль и файлы

## 🎯 Пример вывода:

```
📁 Найдено Carrd сайтов: 1
🎯 Цель: 200 форм на сайт
📦 Парсинг: 5 объявлений на итерацию

🔄 ИТЕРАЦИЯ 1
🔍 Парсим 5 объявлений...
✅ Спарсено 5 email
🔎 Валидируем 5 email...
✅ Валидных email: 4/5
🔗 Генерируем персональные ссылки для 4 email...
✅ Успешно сгенерировано ссылок: 4/4

📧 [mysite.json] Отправка 4 форм (0/200)
✅ Форма отправлена на user1@gmail.com
🔗 Ссылка: poshmarkab12.domain.com/xyz789
...
```

## 🔧 Проверка результатов:

После запуска проверьте:
1. Email получателей - пришли ли письма через Carrd
2. Папку `logs/` - логи работы
3. Статистику в консоли

## ⚠️ Типичные проблемы и решения

### Проблема: "Не найдено конфигураций сайтов"
**Решение:** Убедитесь, что в папке `carrd_sites/` есть `.json` файлы (не `.example`)

### Проблема: Формы не отправляются
**Решение:** 
1. Проверьте правильность `form_action` URL
2. Убедитесь в корректности названий полей
3. Проверьте не требует ли форма дополнительных полей (антиспам токены и т.д.)

### Проблема: Ошибка 403/401
**Решение:** Добавьте необходимые поля защиты в `additional_fields` конфигурации

### Проблема: "API не вернул ссылку"
**Решение:** Проверьте правильность `WORKER_ID`, `API_URL` и `LINK_SERVICE` в `.env`

## 📚 Дополнительная информация

- Полная документация: `README_CARRD.md`
- Примеры конфигураций: `carrd_sites/example_site.json.example`
- Настройки: `.env.example`

## 💡 Советы

1. **Начните с малого**: Сначала протестируйте на 1-2 формах
2. **Используйте несколько сайтов**: Распределите нагрузку между разными Carrd сайтами
3. **Настройте таймауты**: Увеличьте `SEND_TIMEOUT` если Carrd блокирует частые запросы
4. **Следите за логами**: Они помогут понять что идет не так

---

**Готово! Теперь вы можете запускать Carrd версию софта. Удачи! 🚀**
