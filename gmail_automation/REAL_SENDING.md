# 🔧 Как настроить реальную отправку писем

## ⚠️ Важная информация

Cookie от Gmail **НЕ МОГУТ** использоваться для отправки писем через SMTP или Gmail API.

Cookies дают доступ только к веб-интерфейсу Gmail, но не предоставляют учетные данные для:
- ❌ SMTP сервера
- ❌ Gmail API
- ❌ Прямой отправки

## ✅ Рабочие решения

### Вариант 1: App Password (РЕКОМЕНДУЕТСЯ - самый простой)

**Что нужно:**
1. Gmail аккаунт
2. Включить 2FA (двухфакторную аутентификацию)
3. Создать App Password

**Как настроить:**

1. Перейдите: https://myaccount.google.com/apppasswords
2. Войдите в свой Gmail
3. Создайте App Password для "Mail"
4. Скопируйте пароль (16 символов без пробелов)

**Как использовать:**

Создайте файл `gmail_automation/accounts.json`:

```json
[
  {
    "email": "your.email@gmail.com",
    "app_password": "abcd efgh ijkl mnop"
  },
  {
    "email": "second.email@gmail.com",
    "app_password": "wxyz 1234 5678 9012"
  }
]
```

**Скрипт автоматически использует эти данные для SMTP!**

---

### Вариант 2: Gmail API (для продвинутых)

**Что нужно:**
1. Google Cloud Project
2. Включить Gmail API
3. Создать OAuth2 credentials
4. Скачать credentials.json

**Как настроить:**

1. Перейдите: https://console.cloud.google.com/
2. Создайте новый проект
3. Включите Gmail API
4. Создайте OAuth 2.0 Client ID
5. Скачайте `credentials.json`
6. Положите в `gmail_automation/credentials.json`

**Установите библиотеки:**
```bash
pip install google-auth google-auth-oauthlib google-auth-httplib2 google-api-python-client
```

---

### Вариант 3: Browser Automation (сложный, но работает с cookies)

**Что нужно:**
1. Selenium или Playwright
2. Chrome/Firefox драйвер

**Установка:**
```bash
pip install selenium webdriver-manager
```

**Минусы:**
- Медленно (открывает браузер для каждой отправки)
- Требует много ресурсов
- Может быть обнаружен Gmail

---

## 🚀 Быстрый старт с App Password

**1. Получите App Password (2 минуты):**
- Откройте https://myaccount.google.com/apppasswords
- Создайте пароль для "Mail"
- Скопируйте (например: `abcd efgh ijkl mnop`)

**2. Создайте файл accounts.json:**

```bash
cd gmail_automation
cat > accounts.json << 'EOF'
[
  {
    "email": "ваш.email@gmail.com",
    "app_password": "вставьте сюда app password"
  }
]
EOF
```

**3. Запустите скрипт:**

```bash
python gmail_sender.py
```

**Готово!** Теперь письма будут отправляться реально!

---

## 📋 Проверка

После настройки App Password запустите демо:

```bash
python demo.py
```

Вы увидите:
```
✅ Найден аккаунт: ваш.email@gmail.com
✅ SMTP аутентификация успешна
✅ Письмо отправлено на test@example.com
```

---

## ❓ Частые вопросы

**Q: Можно ли использовать только cookies?**
A: Нет. Cookies работают только для веб-интерфейса, не для отправки.

**Q: Безопасно ли использовать App Password?**
A: Да, это официальный метод Google для приложений. Можно отозвать в любой момент.

**Q: Сколько писем можно отправить?**
A: Gmail лимит: ~500 писем/день для обычного аккаунта.

**Q: Нужен ли App Password для каждого аккаунта?**
A: Да, для каждого Gmail аккаунта нужен свой App Password.

---

## 🔒 Безопасность

**App Passwords:**
- ✅ Официальный метод Google
- ✅ Можно отозвать в любой момент
- ✅ Не дает доступ ко всему аккаунту
- ⚠️ Храните в безопасности (как пароль)

**accounts.json:**
- ⚠️ Добавлен в .gitignore
- ⚠️ Не загружайте в Git
- ⚠️ Храните локально

---

**Выберите Вариант 1 (App Password) - это самое простое и рабочее решение!**
