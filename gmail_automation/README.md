# 📧 Gmail Automation Script

Автоматическая отправка писем через Gmail с использованием cookies и интерактивным управлением через терминал.

## 🎯 Возможности

- ✅ **Авторизация по Cookie** - Вход в Gmail через сохраненные cookie файлы
- ✅ **Множественные аккаунты** - Поддержка неограниченного количества Gmail аккаунтов
- ✅ **Ротация аккаунтов** - Автоматическое переключение между аккаунтами (1-й акк → 1-й email, 2-й акк → 2-й email, и т.д.)
- ✅ **Поддержка прокси** - Работа через прокси серверы для безопасности
- ✅ **Интерактивное управление** - Управление через терминал в реальном времени
- ✅ **Проверка ответов** - Автоматическое уведомление о новых письмах
- ✅ **Inbox режим** - Отправка на конкретный email по требованию
- ✅ **Статистика** - Отслеживание отправленных писем и ошибок

## 📋 Требования

- Python 3.7 или выше
- Библиотека `requests`

## ⚙️ Установка

### 1. Установите зависимости

```bash
pip install requests
```

Или используйте `uv` (рекомендуется):

```bash
uv pip install requests
```

### 2. Структура директорий

После установки у вас будет следующая структура:

```
gmail_automation/
├── gmail_sender.py           # Главный скрипт
├── src/                      # Модули скрипта
│   ├── gmail_client.py       # Gmail клиент с cookie авторизацией
│   ├── email_manager.py      # Менеджер email адресов и аккаунтов
│   ├── proxy_manager.py      # Менеджер прокси
│   ├── message_template.py   # Обработка шаблонов сообщений
│   └── response_checker.py   # Проверка новых ответов
├── cookies/                  # 📁 СЮДА ДОБАВЬТЕ COOKIE ФАЙЛЫ
│   ├── account1.json
│   ├── account2.json
│   └── account3.json
├── emails/                   # 📁 СЮДА ДОБАВЬТЕ СПИСОК EMAIL
│   └── emails.txt
├── text/                     # 📁 СЮДА ДОБАВЬТЕ ТЕКСТ СООБЩЕНИЯ
│   └── text.txt
└── proxies/                  # 📁 СЮДА ДОБАВЬТЕ ПРОКСИ (опционально)
    └── proxies.txt
```

## 🔧 Настройка

### 1. Добавьте Cookie файлы

В папку `cookies/` добавьте JSON файлы с cookie вашех Gmail аккаунтов.

**Формат cookie файла:**

```json
[
  {
    "name": "SID",
    "value": "ваше_значение_cookie",
    "domain": ".google.com"
  },
  {
    "name": "HSID",
    "value": "ваше_значение_cookie",
    "domain": ".google.com"
  },
  {
    "name": "SSID",
    "value": "ваше_значение_cookie",
    "domain": ".google.com"
  }
]
```

**Как получить cookie:**
1. Откройте браузер и войдите в Gmail
2. Откройте DevTools (F12)
3. Перейдите на вкладку Application → Cookies
4. Скопируйте все cookie для домена `.google.com`
5. Сохраните в формате JSON как показано выше

**Альтернативный формат (простой словарь):**
```json
{
  "SID": "значение",
  "HSID": "значение",
  "SSID": "значение"
}
```

Вы можете добавить **сколько угодно аккаунтов** - просто создайте больше файлов:
- `cookies/account1.json`
- `cookies/account2.json`
- `cookies/account3.json`
- и т.д.

### 2. Добавьте список email адресов

Создайте файл `emails/emails.txt` с email адресами получателей (один на строку):

```
user1@example.com
user2@example.com
user3@example.com
user4@example.com
```

### 3. Создайте текст сообщения

Создайте файл `text/text.txt` с текстом вашего сообщения:

```
Привет!

Это тестовое сообщение.

С уважением,
Ваше Имя
```

### 4. Настройте прокси (опционально)

Создайте файл `proxies/proxies.txt` с прокси (один на строку):

```
http://user:password@proxy.example.com:8080
123.45.67.89:8080:username:password
123.45.67.89:8080
```

Поддерживаемые форматы:
- `http://user:pass@host:port`
- `host:port:user:pass`
- `host:port`

Если файл прокси пуст или отсутствует, скрипт будет работать без прокси.

## 🚀 Запуск

### Базовый запуск

```bash
cd gmail_automation
python gmail_sender.py
```

Или с использованием `python3`:

```bash
python3 gmail_sender.py
```

### Что происходит при запуске

1. ✅ Загружаются все cookie файлы из папки `cookies/`
2. ✅ Загружается список email из `emails/emails.txt`
3. ✅ Загружается текст сообщения из `text/text.txt`
4. ✅ Загружаются прокси из `proxies/proxies.txt` (если есть)
5. ✅ Начинается автоматическая отправка:
   - 1-й аккаунт отправляет на 1-й email
   - 2-й аккаунт отправляет на 2-й email
   - 3-й аккаунт отправляет на 3-й email
   - Когда аккаунты заканчиваются, возвращаемся к 1-му аккаунту
   - Продолжаем пока не закончатся email адреса

## 🎮 Интерактивное управление

Во время работы скрипта вы можете управлять им через клавиши:

### Команды

| Клавиша | Действие |
|---------|----------|
| **I** | **Inbox режим** - Приостановить автоматическую отправку и отправить на конкретный email |
| **R** | **Resume** - Возобновить автоматическую отправку |
| **Q** | **Quit** - Выход из скрипта |
| **Ctrl+C** | Экстренная остановка |

### Пример использования Inbox режима

1. Скрипт работает в автоматическом режиме
2. Вы нажимаете клавишу **I**
3. Скрипт останавливается и спрашивает: `Введите email получателя:`
4. Вы вводите email, например: `test@example.com`
5. Скрипт отправляет письмо на этот email
6. Вы нажимаете **R** чтобы возобновить автоматическую отправку

### Проверка ответов

Скрипт автоматически проверяет новые письма каждые 60 секунд и выдает уведомление:

```
============================================================
🔔 НОВОЕ ПИСЬМО!
============================================================
От: user@example.com
Тема: Re: Ваше сообщение
Время: 2025-12-25 12:30:45
============================================================
```

## 🔄 Логика ротации аккаунтов

Скрипт умно распределяет отправку между аккаунтами:

```
Аккаунты: account1.json, account2.json, account3.json
Emails: user1@, user2@, user3@, user4@, user5@

Отправка:
1. account1.json → user1@
2. account2.json → user2@
3. account3.json → user3@
4. account1.json → user4@  (цикл начинается заново)
5. account2.json → user5@
```

Это позволяет:
- ✅ Равномерно распределить нагрузку
- ✅ Уменьшить риск блокировки аккаунтов
- ✅ Максимизировать скорость отправки

## 📊 Статистика

В конце работы скрипт показывает статистику:

```
============================================================
📊 Статистика
============================================================
Отправлено успешно: 45
Ошибки: 2
Всего обработано: 47
============================================================
```

## 🛠️ Улучшения и дополнительные возможности

### 1. Персонализация сообщений

Вы можете использовать переменные в тексте сообщения. Например, в `text/text.txt`:

```
Привет!

Это письмо отправлено на {EMAIL}.

С уважением
```

Переменная `{EMAIL}` будет автоматически заменена на адрес получателя.

### 2. Задержка между отправками

Скрипт автоматически делает паузу 2 секунды между отправками, чтобы не перегружать серверы.

### 3. Обработка ошибок

Если отправка не удалась, скрипт:
- ⚠️ Записывает ошибку
- ⚠️ Переходит к следующему email
- ⚠️ Продолжает работу

### 4. Поддержка множества форматов прокси

Скрипт поддерживает различные форматы прокси и автоматически их обрабатывает.

## ⚠️ Важные замечания

### ✅ ОБНОВЛЕНИЕ: Теперь поддерживается реальная отправка!

**Новая версия с РЕАЛЬНОЙ отправкой:** `gmail_sender_real.py`

**Что изменилось:**
- ✅ Добавлена поддержка Gmail App Passwords
- ✅ Реальная отправка через SMTP
- ✅ Простая настройка (2 минуты)

**Как использовать:**

1. **Получите App Password:**
   - Откройте: https://myaccount.google.com/apppasswords
   - Создайте пароль для "Mail"
   - Скопируйте (например: `abcd efgh ijkl mnop`)

2. **Создайте файл `accounts.json`:**
   ```json
   [
     {
       "email": "ваш.email@gmail.com",
       "app_password": "abcd efgh ijkl mnop"
     }
   ]
   ```

3. **Запустите:**
   ```bash
   python gmail_sender_real.py
   ```

**📖 Полная инструкция:** См. [REAL_SENDING.md](REAL_SENDING.md)

---

### ⚠️ О старой версии (gmail_sender.py)

Старый скрипт `gmail_sender.py` использует только cookies и работает в ДЕМО режиме.

**Почему cookies не работают для отправки:**
- Cookies дают доступ только к веб-интерфейсу
- Gmail не поддерживает cookie-auth для SMTP или API
- Нужны App Passwords или OAuth2

### Безопасность

1. **Не делитесь cookie файлами** - они дают полный доступ к вашему аккаунту
2. **Используйте прокси** - для дополнительной безопасности
3. **Храните cookie в безопасном месте** - не коммитьте в Git

### Ограничения Gmail

1. **Лимиты отправки** - Gmail имеет лимиты на количество писем в день (~500 писем)
2. **Спам фильтры** - массовая отправка может быть расценена как спам
3. **Задержки** - рекомендуется делать паузы между отправками

### Рекомендации

1. ✅ Используйте задержки между отправками (уже встроено)
2. ✅ Не отправляйте больше 100-200 писем с одного аккаунта в день
3. ✅ Используйте несколько аккаунтов для распределения нагрузки
4. ✅ Периодически меняйте прокси
5. ✅ Следите за блокировками аккаунтов

## 🐛 Troubleshooting

### Проблема: Не загружаются cookie

**Решение:**
- Проверьте формат JSON файла
- Убедитесь что cookie актуальные (не истекли)
- Проверьте что cookie скопированы из правильного домена (`.google.com`)

### Проблема: Не отправляются письма

**Решение:**
- Проверьте интернет соединение
- Проверьте прокси (если используются)
- Убедитесь что аккаунт не заблокирован
- Попробуйте войти в Gmail вручную с теми же cookie

### Проблема: Скрипт не реагирует на клавиши

**Решение:**
- На Windows это работает автоматически
- На Linux/Mac может потребоваться запуск в специальном режиме терминала
- Попробуйте запустить с `python3 -u gmail_sender.py`

### Проблема: Ошибка импорта модулей

**Решение:**
```bash
# Убедитесь что находитесь в правильной директории
cd gmail_automation

# Проверьте что все файлы на месте
ls -la src/

# Запустите снова
python gmail_sender.py
```

## 📝 Примеры использования

### Пример 1: Простая отправка

```bash
# 1. Добавьте 1 cookie файл
cookies/account1.json

# 2. Добавьте 3 email
emails/emails.txt:
  user1@example.com
  user2@example.com
  user3@example.com

# 3. Добавьте текст
text/text.txt:
  Привет! Это тестовое сообщение.

# 4. Запустите
python gmail_sender.py
```

### Пример 2: Массовая отправка с ротацией

```bash
# 1. Добавьте 3 cookie файла
cookies/account1.json
cookies/account2.json
cookies/account3.json

# 2. Добавьте 100 email в emails.txt

# 3. Добавьте прокси
proxies/proxies.txt:
  http://proxy1.com:8080
  http://proxy2.com:8080

# 4. Запустите
python gmail_sender.py

# Результат: каждый аккаунт отправит ~33 письма
```

### Пример 3: Inbox режим

```bash
# 1. Запустите скрипт
python gmail_sender.py

# 2. Во время работы нажмите "I"
# 3. Введите email: urgent@example.com
# 4. Письмо отправится немедленно
# 5. Нажмите "R" для продолжения автоматической отправки
```

## 🔄 Обновления

### Версия 1.0
- ✅ Базовая функциональность
- ✅ Cookie авторизация
- ✅ Ротация аккаунтов
- ✅ Поддержка прокси
- ✅ Интерактивное управление
- ✅ Проверка ответов

## 📄 Лицензия

Этот скрипт предоставляется "как есть" для личного использования.

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел Troubleshooting
2. Убедитесь что все файлы настроены правильно
3. Проверьте логи и сообщения об ошибках

---

**Удачной работы! 🚀**
